# Argos — enable NetworkManager periodic WAN connectivity check.
# Drives connectivity-change events into /etc/NetworkManager/dispatcher.d/.
# Probes every 30s; HTTP 204 from connectivitycheck.gstatic.com = FULL connectivity.
[connectivity]
uri=http://connectivitycheck.gstatic.com/generate_204
interval=30
response=
