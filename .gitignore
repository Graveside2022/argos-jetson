# ============================================================
# Argos .gitignore — SDR & Network Analysis Console
# Organized by category. No duplicates. No dead patterns.
# Last audit: 2026-02-20
# ============================================================

# ── Dependencies ─────────────────────────────────────────────
node_modules/
.setup-cache/

# Graphify AST knowledge-graph output + Serena LSP session
graphify-out/
.serena/
.pnp
.pnp.js
vendor/
jspm_packages/
web_modules/
bower_components/
.npm/
.yarn/*
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
pnpm-lock.yaml

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
pip-log.txt
.venv/
venv/
env/
ENV/
virtualenv/
*.egg-info/
*.egg

# ── Build Output ─────────────────────────────────────────────
/build/
build/Release/
dist/
.svelte-kit/
package/

# ── Environment Variables ────────────────────────────────────
.env
.env.*
!.env.example
.envrc

# ── Logs ─────────────────────────────────────────────────────
logs/
*.log
*.debug

# ── Editors & IDEs ───────────────────────────────────────────
.vscode/*
!.vscode/extensions.json
!.vscode/tasks.json
!.vscode/launch.json
.vscode-test/
.idea/
.project
.classpath
.c9/
.nova/
*.sublime-project
*.sublime-workspace

# ── OS Files ─────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ── Temporary & Cache Files ──────────────────────────────────
*.tmp
*.temp
*.cache
.temp/
.tmp/
tmp/
temp/
.cache/
.eslintcache
.stylelintcache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# ── Swap & Backup Files ─────────────────────────────────────
*.swp
*.swo
.*.swp
.*.swo
*~
*.backup
*.bak
*.orig
*.md.backup
*.working
*.broken

# ── Vite ─────────────────────────────────────────────────────
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
*.timestamp-*.mjs

# ── TypeScript ───────────────────────────────────────────────
*.tsbuildinfo
.tsc-cache/

# ── Testing ──────────────────────────────────────────────────
coverage/
*.lcov
.nyc_output/
.pytest_cache/
htmlcov/
.tox/
.nox/
.hypothesis/
.coverage
.coverage.*
lib-cov/
test-results/
playwright-report/
playwright/.cache/
tests/visual/screenshots/
tests/visual/diffs/
tests/visual/baselines/
tests/reports/
browser-test.png

# ── Runtime Data ─────────────────────────────────────────────
pids/
*.pid
*.seed
*.pid.lock
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json
.node_repl_history

# ── Archives & Packed Files ──────────────────────────────────
*.zip
*.tgz
*.tar
*.tar.gz
*.tar.bz2

# ── Database Files ───────────────────────────────────────────
*.db
*.sqlite
*.db-shm
*.db-wal
*.db.backup-*

# ── Certificates & Credentials (SECURITY) ───────────────────
*.p12
*.pem
*.key
*.crt
*.csr
*.jks
*.keystore
*.pfx
config/opencellid.json
.mcp.json

# ── Kismet Capture Files ────────────────────────────────────
*.kismet
*.kismet-journal
*.kismetdb

# ── Core Dumps (numeric PID suffix only) ────────────────────
core.[0-9]*

# ── Docker ───────────────────────────────────────────────────
docker-images/

# ── Runtime Data Directories ────────────────────────────────
/data/
database/
service/
tools/
completed/

# ── AI Development Tools ────────────────────────────────────
.gemini/
.agent/
.development-archive/
_bmad/
_bmad-output/
CLAUDE.local.md
# ── Claude Code ──────────────────────────────────────────────
# CLAUDE.md is tracked — it's the primary AI context file for the project.
# .claude/ contains local settings (API keys, permissions) — never shared.
.claude/

# ── SpecifyKit ───────────────────────────────────────────────
# Dev specs/plans generated locally, not shipped with product.
.specify/

# ── Internal Dev Artifacts ───────────────────────────────────
# Development research, specs, and prompts — tracked for backup.
# plans/
# specs/
# docs/prompts/
# docs/plans/

# ── Husky Internals (only .husky/pre-commit is tracked) ────
.husky/_/

# ── Project-Specific: Ephemeral Dev Artifacts ───────────────
# One-off scripts generated during dev sessions (root-only)
/claude-*.sh
/test-*.sh
/test-*.js
/test-*.cjs
/test-*.html

# Generated reports
audit-report.json
css-integrity-baselines.json
css-integrity-report.json
eslint_*.json
page_eslint_results.json
hooks.json

# Legacy one-off CJS scripts (root-only)
/check_eslint.cjs
/quick_eslint_count.cjs
/simple_validate.cjs
/validate_mission_control.cjs

# Build framework generated reports
tests/visual/pi-baseline-report.json

# Autotools artifacts (root-only)
/ltmain.sh

# Debug/test utilities (not part of main app)
utils/debug/
utils/python/

# Console debug output
console.debug.txt
console.debug.*

# ── Project-Specific: Historical Junk Patterns ──────────────
# Catch-all patterns for legacy dev artifacts that may reappear.
# These were one-off files from early development — patterns
# kept as safety nets.
Initiative_*.md
initiative*.md
notes_*.md
debug_*.md
troubleshooting_*.md
TASKS*.md
TASK_*.md
STRATEGY*.md
SSR_*.md
FAILED_*.md
PLAN_*.md
TODO*.md
schema_*.txt
schema_*.md
current_schema*.sql
fk_*.txt
hookplanning.txt
indices_list.txt
tables_list.txt
dependency_audit.txt
dependencies_out_of_date.txt
software_requirements*.md
ESLint_*.txt
hackrfbackup.*

# Large data files
map-data/*.csv

/.quarto/
**/*.quarto_ipynb

# Blue Dragon — Rust BLE sniffer artifacts ignored; first-party Python tooling tracked
tactical/blue-dragon/target/
tactical/blue-dragon/Cargo.lock
tactical/blue-dragon/crates/
!tactical/blue-dragon/tools/

# Remediation rollback snapshots (local-only pre-flight state)
.rollback/

# Security — IDE settings with secrets
.vscode/settings.json

# Dev tooling (not project code)
claude-code-lsp-enforcement-kit/

# Binary artifacts (distribute via releases)
*.apk
